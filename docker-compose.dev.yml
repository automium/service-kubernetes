---

version: '3'
services:
  deploy:
    container_name: automium-service-kubernetes-dev
    image: automium-service-kubernetes-dev
    build:
      context: .
    volumes:
      - "/tmp/${CONFIG}_terraform:/usr/src/provisioner/.terraform"
