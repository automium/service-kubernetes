- name: disable-kubernetes-dashboard | disable kubernetes dashboard globally
  lineinfile: 
    path: /usr/src/cloud/kubespray/inventory/{{ cluster_name }}/group_vars/k8s-cluster/addons.yml 
    regexp: '^dashboard_enabled: true' 
    line: 'dashboard_enabled: false' 
