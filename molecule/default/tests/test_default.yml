---
package:
  kubernetes:
    installed: true
port:
  tcp:80:
    listening: true
    ip:
      - 0.0.0.0
service:
  kubernetes:
    enabled: true
    running: true
user:
  kubernetes:
    exists: true
    groups:
      - kubernetes
group:
  kubernetes:
    exists: true
process:
  kubernetes:
    running: true
